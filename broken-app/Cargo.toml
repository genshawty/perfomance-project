[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "baseline"
harness = false

[[bench]]
name = "criterion"
harness = false

[profile.release]
debug = 1